<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/layout_adapterEntry"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="1dp"
    android:clickable="true">

    <org.tmcrafz.flipiconchecker.FlipIconChecker
        android:id="@+id/flipChecker_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:frontView="@layout/flip_icon_checker_view"
        app:backView="@layout/flip_icon_checker_view"
        app:checkView="@layout/flip_icon_checker_view" />
    <!--
    <ImageView
        android:id="@+id/imageView_appIcon"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@mipmap/ic_launcher" />
    -->
    <TextView
        android:id="@+id/txt_appName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="Name"
        android:layout_marginStart="12dp"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@+id/flipChecker_icon" />
</RelativeLayout>